# Makefile for Docker image: rig-ubuntu-duckdb4
# Generated by generate_stages.py, do not edit by hand
# Run 'make generate-makefiles' to regenerate this file
# Supported architectures: amd64

.PHONY: build pull pull-parent clean help build-amd64 pull-amd64 pull-parent-amd64 run-amd64 run-root-amd64 clean-amd64

# Default target
help:
	@echo "Available targets for rig-ubuntu-duckdb4:"
	@echo "  build              - Build the Docker image for supported platform(s)"
	@echo "  build-amd64        - Build the Docker image for amd64 platform"
	@echo "  pull               - Pull the Docker image (manifest) from registry"
	@echo "  pull-amd64         - Pull the amd64 Docker image from registry"
	@echo "  pull-parent-amd64  - Pull the amd64 parent image this image depends on"
	@echo "  run-amd64          - Run interactive bash as regular user (amd64)"
	@echo "  run-root-amd64     - Run interactive bash as root user (amd64)"
	@echo "  clean-amd64        - Remove the amd64 Docker image"
	@echo "  help               - Show this help message"

# Build the Docker image for supported platform(s)
build: build-amd64

# Build the Docker image for amd64 platform
build-amd64:
	@echo "Building Docker image for amd64: ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64"
	@echo "Creating date file for cache invalidation..."
	@date -Idate > date.txt
	@trap 'rm -f date.txt' EXIT; docker build --platform linux/amd64 --build-arg TARGETARCH=amd64 -t ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64 .

# Pull the Docker image from registry (manifest)
pull:
	@echo "Pulling Docker image: ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest"
	docker pull ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest

# Pull the amd64 Docker image from registry
pull-amd64:
	@echo "Pulling amd64 Docker image: ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64"
	docker pull ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64

# Pull the parent image this image depends on
pull-parent:
	@echo "Pulling parent image: ghcr.io/cynkra/docker-images/rig-ubuntu:latest"
	docker pull ghcr.io/cynkra/docker-images/rig-ubuntu:latest

# Pull the amd64 parent image this image depends on
pull-parent-amd64:
	@echo "Pulling amd64 parent image: ghcr.io/cynkra/docker-images/rig-ubuntu:latest"
	docker pull --platform linux/amd64 ghcr.io/cynkra/docker-images/rig-ubuntu:latest

# Run interactive bash as regular user (amd64)
run-amd64:
	@echo "Starting interactive bash session (amd64) in ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest"
	docker run --platform linux/amd64 --rm -it ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest /bin/bash

# Run interactive bash as root user (amd64)
run-root-amd64:
	@echo "Starting interactive bash session as root (amd64) in ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest"
	docker run --platform linux/amd64 --rm -it --user root ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest /bin/bash

# Remove the Docker image (supported architectures)
clean: clean-amd64

# Remove the amd64 Docker image
clean-amd64:
	@echo "Removing amd64 Docker image: ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64"
	docker rmi ghcr.io/cynkra/docker-images/rig-ubuntu-duckdb4:latest-amd64 || true
