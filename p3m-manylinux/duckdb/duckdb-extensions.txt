# duckdb extensions to INSTALL and LOAD when the image is built.
# One extension name per line. Blank lines and lines starting with '#' are
# ignored. This file is part of the build context and is read by the Dockerfile.
#
# Building fails if any listed extension has no prebuilt binary for this
# image's OS/architecture, so the image doubles as a per-distro check that
# duckdb extensions are installable. Successfully installed extensions are
# cached into the image at $DUCKDB_EXTENSION_DIRECTORY.
httpfs
json
parquet
icu
fts
excel
spatial
sqlite
inet
tpch
